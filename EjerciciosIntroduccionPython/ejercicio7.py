# 7-Escribe un programa que calcule el área de un triángulo a partir de la base y la
# altura dadas.

base = float(input("introduce la base del triangulo: "))

altura = float(input("introduce la altura del triangulo: "))

areaTriangulo = (base * altura) / 2

print(f"El area de este triangulo es {areaTriangulo}")